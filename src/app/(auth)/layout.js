
const AuthLayout = ({children}) => {
    return (
        <div>
            <h2 className="text-3xl text-red-500 text-center font-bold">Auth Layout</h2>
            {children}
        </div>
    );
};

export default AuthLayout;
